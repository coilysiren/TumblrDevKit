variables
    {Title}

RE_ASSIGNMENT = re.compile(r'^(-\s+var\s+)?(\w+) += *([^;\n]+)( *;? *)')
inp = 'mew'
match = RE_ASSIGNMENT.match(inp)
if RE_ASSIGNMENT.match(): inp[match.start():match.end()]

content blocks
    {block:Text}{/block:Text}

conditional blocks
    {block:PreviousPage}{/block:PreviousPage}
